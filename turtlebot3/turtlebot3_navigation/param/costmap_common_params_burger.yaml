obstacle_range: 3.0  # 장애물로 간주되는 최대 거리 (미터)

raytrace_range: 3.5  # 레이트레이스 최대 거리 (미터)

footprint: [[-0.04, -0.04], [-0.04, 0.04], [0.04, 0.04], [0.04, -0.04]]
# robot_radius: 0.105


inflation_radius: 0.1  # 장애물 팽창 반지름 (미터)

cost_scaling_factor: 300.0  # 코스트 값에 곱해질 스케일링 팩터

map_type: costmap
observation_sources: scan
scan: {sensor_frame: base_scan, data_type: LaserScan, topic: scan, marking: true, clearing: true}